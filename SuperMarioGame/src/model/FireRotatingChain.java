package model;

public class FireRotatingChain extends Villain{
    public FireRotatingChain(String name, int damage, boolean isMortal, MoveTypeEnum moveTypeEnum, Coordinate coordinate, int health) {
        super(name, damage, isMortal, moveTypeEnum, coordinate, health);
    }
}
